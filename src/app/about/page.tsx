import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "About",
  description: "about description",
};

const page = () => {
  return <div>Default function about</div>;
};

export default page;
